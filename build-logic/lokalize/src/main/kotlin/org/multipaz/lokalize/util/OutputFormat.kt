package org.multipaz.lokalize.util

/**
 * Output format for localization files.
 */
enum class OutputFormat {
    /** Android XML format (strings.xml) */
    XML,

    /** JSON format (strings.json) */
    JSON
}
